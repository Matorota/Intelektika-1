═══════════════════════════════════════════════════════════════════════
   📤 KAIP ĮKELTI Į GITHUB - ŽINGSNIS PO ŽINGSNIO
═══════════════════════════════════════════════════════════════════════

PROBLEMA: Neturite nustatyto "origin" remote repozitorijos.

╔═══════════════════════════════════════════════════════════════════╗
║  SPRENDIMAS (Pasirinkite vieną iš 2 būdų):                       ║
╚═══════════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────────┐
│ BŪDAS 1: Sukurti naują GitHub repozitoriją                       │
└───────────────────────────────────────────────────────────────────┘

1. Eikite į: https://github.com/new

2. Sukurkite naują repozitoriją:
   - Repository name: intelektika-1
   - Description: Neinformuotos paieškos algoritmai (DFS vs BFS)
   - Public arba Private (jūsų pasirinkimas)
   - ❌ NEPAŽYMĖKITE "Initialize with README" (jau turite!)

3. Terminale įvykdykite:

   git remote add origin https://github.com/JŪSŲ_USERNAME/intelektika-1.git
   git branch -M main
   git push -u origin main

   ARBA su SSH (jei turite nustatę):

   git remote add origin git@github.com:JŪSŲ_USERNAME/intelektika-1.git
   git branch -M main
   git push -u origin main


┌───────────────────────────────────────────────────────────────────┐
│ BŪDAS 2: Naudoti esamą repozitoriją                              │
└───────────────────────────────────────────────────────────────────┘

Jei jau turite GitHub repozitoriją:

   git remote add origin https://github.com/JŪSŲ_USERNAME/JŪSŲ_REPO.git
   git branch -M main
   git push -u origin main


┌───────────────────────────────────────────────────────────────────┐
│ PILNAS PAVYZDYS                                                   │
└───────────────────────────────────────────────────────────────────┘

# 1. Patikrinti ar esate git repozitorijoje
git status

# 2. Jei ne, inicializuoti:
git init

# 3. Pridėti visus failus:
git add .

# 4. Commit:
git commit -m "Pradinė versija: DFS vs BFS palyginimas"

# 5. Pridėti remote (pakeiskite USERNAME į savo):
git remote add origin https://github.com/USERNAME/intelektika-1.git

# 6. Push:
git branch -M main
git push -u origin main


┌───────────────────────────────────────────────────────────────────┐
│ PATIKRINTI AR VEIKIA                                              │
└───────────────────────────────────────────────────────────────────┘

# Po šių komandų turėtų veikti:
git remote -v

# Turėtumėte matyti:
# origin  https://github.com/USERNAME/intelektika-1.git (fetch)
# origin  https://github.com/USERNAME/intelektika-1.git (push)


┌───────────────────────────────────────────────────────────────────┐
│ KĄ DARYTI JEIGU PRAŠO PRISIJUNGTI                                │
└───────────────────────────────────────────────────────────────────┘

GitHub nebe leidžia naudoti slaptažodžio, reikia:

1. Personal Access Token (PAT):
   - Eikite: https://github.com/settings/tokens
   - Generate new token (classic)
   - Pasirinkite "repo" scope
   - Nukopijuokite token
   - Naudokite kaip slaptažodį

2. ARBA SSH key:
   - Sukurkite: ssh-keygen -t ed25519 -C "your_email@example.com"
   - Pridėkite į GitHub: https://github.com/settings/keys
   - Naudokite SSH URL: git@github.com:USERNAME/REPO.git


┌───────────────────────────────────────────────────────────────────┐
│ GREITAS SETUP SCRIPT                                              │
└───────────────────────────────────────────────────────────────────┘

Paleiskite (pakeiskite USERNAME):

#!/bin/bash
USERNAME="jūsų-github-username"
REPO="intelektika-1"

git remote add origin https://github.com/$USERNAME/$REPO.git
git branch -M main
git push -u origin main


═══════════════════════════════════════════════════════════════════════
💡 REKOMENDACIJA:
═══════════════════════════════════════════════════════════════════════

1. Eikite į https://github.com/new
2. Sukurkite "intelektika-1" repozitoriją
3. Terminale įvykdykite (pakeiskite USERNAME):

   git remote add origin https://github.com/USERNAME/intelektika-1.git
   git push -u origin main

═══════════════════════════════════════════════════════════════════════
